# =============================================================================
# Kwik.gg - Performance & Timeout Settings
# Drop this into /etc/nginx/conf.d/kwik-performance.conf
# These settings are specific to kwik.gg and won't conflict with nginx.conf
# =============================================================================

# NOTE: All global directives have been moved to site configs to avoid duplicates
# This file is intentionally minimal - timeouts are set per-site in server blocks
